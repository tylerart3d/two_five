# 2/5 command chronology OCR comparison — 1965–1966

Run: `data/local/ocr-1965-1966-qwen-20260912` (local-only).
Model: M1 Pro Qwen 3.8 Flash Next IQ4XS through AgentRelay.
Scope: 11 existing 2/5 chronology files, 1201048065–1201048075, 663 PDF pages including attachments. Excludes books and ship logs. November 1966 is absent from this sequence; September's existing OCR describes tracking sheets, not a complete chronology.

Each page is transcribed independently from the original PDF without showing Qwen earlier OCR or human corrections. New transcripts are layer 1, never replacements for existing OCR. Requests, raw responses, PDF/image/text hashes, model identity and page numbers are preserved. Truncated/empty responses are rejected and retried; failures remain explicit. Successful pages are skipped when resuming.

`status.json` records progress. Each document receives a reading copy and a whitespace-normalized `comparison.diff` against existing non-archived OCR. The diff is a review aid, not an accuracy score: formatting and inconsistent old page markers also cause differences. Human fixes retain priority. No output is ingested into the website automatically.

Run or resume: `node scripts/rerun-command-ocr.mjs`. All 663 pages must be accounted for before calling this complete. Review differences and report any missing/truncated pages separately.

## September 13 dual-host continuation
Original worker stopped with 241 saved pages; remaining 422 assigned once across `data/local/ocr-split-20260913-m1` and `data/local/ocr-split-20260913-workstation` (211 each). Each manifest stores exact assigned pages, source hashes, and model. Old run is superseded, not deleted. PIDs at launch: 51940 and 20052. Partial reading copies contain assigned pages only; missing unassigned pages in comparisons are not OCR loss. Human fixes remain separate.
